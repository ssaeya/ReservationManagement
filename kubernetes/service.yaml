apiVersion: v1
kind: Service
metadata:
  name: ReservationManagement
  labels:
    app: ReservationManagement
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: ReservationManagement